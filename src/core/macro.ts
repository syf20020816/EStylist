/**
 * ============================================
 * @author:syf20020816@outlook.com
 * @since:20230726
 * @version:0.1.0
 * @type:interface
 * @description:模拟宏实现
 * ============================================
 */

import { ref } from "vue";

/**
 * 模拟生成如：let areaRef0 = ref()
 * @param len
 * @returns
 */
export const buildRef = (): Array<any> => {
  return [];
};
